[build]
builder = "nixpacks"

[deploy]
numReplicas = 1
startCommand = "python migrate.py && gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"

[service]
port = 8080

[[mounts]]
mountPath = "/app/uploads"
repository = "uploads"
