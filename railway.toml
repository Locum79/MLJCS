[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[build.nix]
packages = ["cairo", "pango", "gdk-pixbuf"]

[deploy]
numReplicas = 1
startCommand = "gunicorn wsgi:app --bind 0.0.0.0:8080 --workers 2 --timeout 120"

[service]
port = 8080
