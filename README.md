# CertifyStack

Lean certificate generation and dispatch platform.

## Setup
1. Clone repo
2. Create virtual environment: `python -m venv venv && source venv/bin/activate`
3. Install: `pip install -r requirements.txt`
4. Set environment variables
5. Run: `flask run`

## Deployment (Railway)
`railway up`
